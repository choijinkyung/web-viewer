import { styled } from '@mui/system';

const Error = styled('div')({
  color: '#e01e5a',
  marginTop: 1,
  marginRight: 0,
  marginLeft: 2,
  fontWeight: 'bold',
});

const Success = styled('div')({
  color: '#2eb67d',
  fontWeight: 'bold',
});

export { Error, Success };
